# En bootstrap.properties se agregan solo las propiedades para la configuracion de Spring Cloud Config
# Debemos tener aqui el nombre de la aplicacion
spring.application.name=servicio-pedidos

# URL del servidor de configuracion
spring.cloud.config.uri=http://localhost:8100

# Estamos recuperando el puerto 8012 que sustituye al 8002 (declarado en application.properties)
# La configuracion obtenida por este fichero sobreescribe al application.properties
